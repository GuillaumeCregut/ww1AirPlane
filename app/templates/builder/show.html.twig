{% extends 'base.html.twig' %}

{% block title %}Builder{% endblock %}

{% block body %}
    <h1>Builder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ builder.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ builder.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_builder_index') }}">back to list</a>

    <a href="{{ path('app_builder_edit', {'id': builder.id}) }}">edit</a>

    {{ include('builder/_delete_form.html.twig') }}
{% endblock %}
