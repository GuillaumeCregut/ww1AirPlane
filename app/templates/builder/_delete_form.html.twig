<form method="post" action="{{ path('app_builder_delete', {'id': builder.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ builder.id) }}">
    <button class="btn">Delete</button>
</form>
