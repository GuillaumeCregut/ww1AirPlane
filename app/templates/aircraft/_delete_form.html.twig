<form method="post" action="{{ path('app_aircraft_delete', {'id': aircraft.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cet item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aircraft.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
